# CLAUDE.md

The instructions for this repository are tool-neutral and live in `AGENTS.md`, so every
AI coding agent reads the same rules. Claude Code loads them through the import below.
Edit `AGENTS.md`, not this file.

@AGENTS.md
